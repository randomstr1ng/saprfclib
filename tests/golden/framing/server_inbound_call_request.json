{
  "message_type": "SERVER_INBOUND_CALL_REQUEST",
  "capture_source": "live capture 2026-06-29 via Wireshark; SAP A4H gateway 192.168.88.7:3300 pushing the post-registration inbound exchange to the registered server",
  "evidence": "inbound server receive path (same codepath as the client response read)",
  "ni_header_length": 4,
  "notes": [
    "Inbound dispatch reuses the 06-family GW machinery. First inner frame type 0x060F (GW data) carrying the gateway-assigned handle '36964135' and the peer IP block; a second inner frame 0x0605 (GW_DONE) follows at payload offset 224.",
    "ALL inbound bytes are peer-influenced and untrusted (threat T-05-S02) — parse with the bounds-checked TLV walker only.",
    "The call-type marker the read loop consumes is noted for Phase 6 tRFC/qRFC discrimination; NOT decoded here.",
    "Stored as a reference fixture for the Wave 2 inbound deserializer; not byte-reproduced by this plan, so all content is marked variable."
  ],
  "expected_parse": {
    "first_frame_gw_type": "0x060f",
    "second_frame_gw_type": "0x0605",
    "connection_handle": "36964135"
  },
  "field_annotations": [
    {
      "offset": 4,
      "length": 2,
      "name": "gw_type",
      "value": "0x060f",
      "type": "fixed",
      "note": "GW data frame (inbound)"
    },
    {
      "offset": 0,
      "length": 4,
      "name": "ni_payload_length",
      "value": "",
      "type": "fixed",
      "note": "BE uint32 = 224 (first inner frame)"
    },
    {
      "offset": 6,
      "length": 306,
      "name": "inbound_payload",
      "value": "session-specific",
      "type": "variable",
      "note": "peer-influenced handle/IP/host block + trailing 0x0605 GW_DONE frame; untrusted"
    }
  ]
}
